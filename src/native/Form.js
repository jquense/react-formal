import { View } from 'react-native'; // eslint-disable-line import/no-unresolved

import setDefaults from './setDefaults';
import Form from '../Form';

export default setDefaults(Form, {
  component: View
});
